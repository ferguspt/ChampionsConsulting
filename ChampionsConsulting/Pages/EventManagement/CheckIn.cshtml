@page
@model ChampionsConsulting.Pages.EventManagement.CheckInModel
@{
}
<style>
    body {
        font-family: 'Arial', sans-serif;
        color: #333;
        line-height: 1.6;
        background-image: linear-gradient(#f048c6, #f6ea41 );
        font-family: Verdana, sans-serif;
        margin: 0
    }

    footer {
        position: fixed;
        bottom: 0;
        width: 100%;
        min-height: 50px;
        max-height: 80px;
        background-color: rgba(36, 32, 33, 0.9);
        color: white;
        font-family: "Helvetica Neue", Helvetica;
        padding: 10px 0px;
        padding-top: 10px;
        margin-top: 30px;
        font-weight: 100;
        clear: both;
    }
</style>
Please confirm that you are at the location and click "Check in" to proceed.

<br/>

<button asp-page-handler="CheckInHandler" class="btn btn-primary">Check in</button>

<button asp-page-handler="ReturnHandler" class="btn btn-primary">Return</button>